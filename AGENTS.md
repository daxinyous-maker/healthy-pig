# 健康小猪项目永久规则

本文件是本项目的长期记忆。任何人或 Codex 在修改代码前，都必须先完整阅读本文件和 `docs/HEALTHY_PIG_BASELINE.md`。这些规则优先于从旧提交、旧页面或旧 APK 中推断出的实现方式。

## 1. 不可回退原则

- 用户已经确认的功能视为锁定基线，只能增量修改，不得用旧页面、旧提交或旧模板整体覆盖。
- 修改前先检查当前工作区、最近提交和基线文件；发现历史版本与基线冲突时，以基线为准。
- 网页端和手机端是两个都需要完整实现、分别验收的产品端。不得把其中一端当成另一端的功能基准。
- 不得为了完成一个新要求，私自删掉、隐藏或简化已经确认的功能。

## 2. 数据与隐私

- 健康记录、昵称、头像、计划、目标和习惯只保存在用户当前设备。
- 网页端不得重新接入 `/api/entries`、`/api/profile`、D1 或其他健康数据云端接口。
- 手机端覆盖安装必须保留应用本地数据；不得要求用户先卸载旧版。
- 数据结构升级必须兼容旧数据，不能因更新而清空记录。

## 3. 发布边界

- 未经用户明确授权，不得发布网站、打包 APK、上传文件或更改访问权限。
- “上传”必须区分网页代码和 APK；含义不明确时先确认，不能自行把 APK 放入网页发布包。
- 网页和 APK 永远作为两个独立交付物。APK 默认只保存在本机，不进入网站静态资源。
- 网站默认仅用户本人访问。只有用户明确说“公开发布”时，才允许改成任何人可访问。
- 用户要求“改好让我确认”时，只能打开本机预览；确认前不得发布或打包。

## 4. 完成前的强制检查

- 必须运行 `npm test`，并检查手机端 JavaScript 语法。
- 网页端必须分别按桌面宽度和手机宽度实际打开并点击关键功能。
- 必查：四个导航、提醒、智能输入与自然语言识别、记一笔时间、计划新增/编辑、趋势详情、头像昵称、习惯增删、目标修改。
- 视觉修改必须实际查看结果，不能只凭 CSS 或代码推断完成。
- 只有检查通过后才能向用户说“已修好”或“已完成”。

## 5. 修改方式

- 优先局部修改，避免重写整个页面。
- 每次修改后对照 `docs/HEALTHY_PIG_BASELINE.md`，确认没有回退。
- 用户确认一个版本后，应保存为本机提交；后续工作基于该确认版本继续。
- 如果请求会与锁定基线冲突，先向用户说明冲突并征求决定。
